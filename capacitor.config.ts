import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.zti.nexora',
  appName: 'Nexora',
  webDir: 'out',
  server: {
    androidScheme: 'https',
  },
};

export default config;
